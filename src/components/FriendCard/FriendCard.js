import React from "react";
import "./FriendCard.css";


const FriendCard = props => (
   <div className="card">
    <div className="img-container">
    <button onClick={this.increaseScore}><img alt={props.name} src={props.image} /></button>
    </div>

</div>
);

export default FriendCard;
